node_modules/
.next/
.env*
!.env.example
*.tsbuildinfo
.vercel
coverage/
# Regenerated by `next dev`/`next build`, and the two write different
# variants — tracking it means a permanent dirty diff that flips direction
# with whichever command ran last.
next-env.d.ts
